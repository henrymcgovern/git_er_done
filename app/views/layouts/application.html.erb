<!DOCTYPE html>
<html>
<head>
  <title>git 'er done</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<% if current_user %>
	<p>
		<%= link_to "Create New Task", new_task_path %>
		<%= link_to "Categorize!", categorize_tasks_path %>
		<%= link_to "Git 'er Done!", finish_tasks_path %>
		<%= link_to "Projects", project_tasks_path %>
		<%= link_to "Stuff for later", later_tasks_path %>
		<%= link_to "Full List", list_tasks_path %>
	</p>
<% else %>
	<%#= link_to "Sign Up", signup_path %> or
	<%= link_to "Log In", login_path %>
<% end %>
<hr>

<%= yield %>

<% flash.each do |key, value| %>
    <p><%= key %>: <%= value %></p>
<% end %>
</body>
<br>
<hr>
<% if current_user %>
	Logged in as <strong><%= current_user.name %></strong>.
	<% if current_user.guest? %>
	    <%= link_to "Become a Member", signup_path %>
 	<% else %>
		<%= link_to "Log Out", logout_path, :method => :delete %>
	<% end %>
<% else %>
<% end %>
</html>
